{% extends '@templates/base.twig' %}
{% set htmlTitle = page.title ~ ' - ' ~ site.title %}
{% set pageClass = page.template ? ' --' ~ page.template : ' --' ~ site.page %}

{% block content %}
	<header class="intro">
		<h1>{{ page.title }}</h1>
	</header>
	<div class="text">
		{{ page.text.kirbytext | raw }}
	</div>
{% endblock %}

<!doctype html>
<html lang="{{ site.language() }}">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title>{{ htmlTitle | default('No title') }}</title>

	{{ css(['assets/css/index.css', '@auto']) }}

	<link rel="apple-touch-icon" sizes="180x180" href="/assets/img/favicons/apple-touch-icon.png" />
	<link rel="icon" type="image/png" sizes="32x32" href="/assets/img/favicons/favicon-32x32.png" />
	<link rel="icon" type="image/png" sizes="16x16" href="/assets/img/favicons/favicon-16x16.png" />
	<link rel="manifest" href="/site.webmanifest" />
	<link rel="mask-icon" href="/assets/img/favicons/safari-pinned-tab.svg" color="#ff6f4f" />
	<meta name="msapplication-TileColor" content="#ff6f4f" />
	<meta name="theme-color" content="#2b3b4c" />
</head>

<body>

	<div class="page">

		<header class="header">
			<a class="logo" href="{{ site.url }}">{{ site.title }}</a>

			{% include '@snippets/navigation.twig' %}
		</header>
		
		<main{% if mainClass is defined and mainClass is not null %} class="{{ mainClass }}"{% endif %}>
			{% block content %}
			{% endblock %}
		</main>
		
	</div>

	<footer class="footer">
		<a href="{{ site.url }}">&copy; {{ 'now' | date('Y') }} / {{ site.title }}</a>
<!--

		<?php if ($about = page('about')): ?>
		<nav class="social">

			<?php foreach ($about->social()->toStructure() as $social): ?>
			<a href="<?= $social->url() ?>"><?= $social->platform() ?></a>
			<?php endforeach ?>

		</nav>
		<?php endif ?>
-->
	</footer>
</body>

</html>
